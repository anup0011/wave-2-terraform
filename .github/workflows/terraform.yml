Skip to content
Search or jump to…
Pull requests
Issues
Codespaces
Marketplace
Explore
 
@ajaykumarshah03 
anup0011
/
wave-2-terraform
Public
Fork your own copy of anup0011/wave-2-terraform
Code
Issues
Pull requests
Actions
Projects
Wiki
Security
Insights
Beta Try the new code view
wave-2-terraform/.github/workflows/terraform.yml
@anup0011
anup0011 Update terraform.yml
Latest commit e90b950 4 days ago
 History
 1 contributor
35 lines (35 sloc)  838 Bytes
 

name: Terraform
env:
  project_id: db-cicdpipeline-wave-2
on:
  push:
    branches:
      - main
      - feature/*
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
jobs:
  terraform:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v3
      - name: Auth Google cloud
        uses: google-github-actions/auth@v1
        with:
          token_format: 'access_token'
          workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
          service_account: ${{ secrets.SA }}
      - name: Run terraform init
        run: |
          terraform init
      - name: Run terraform plan
        run: |
          terraform plan 
      - name: Run terraform apply
        if: github.ref == 'refs/heads/main'
        run: |
          terraform apply -auto-approve
